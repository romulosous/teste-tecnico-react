import { Meta, StoryObj } from '@storybook/react'

import Loading from '.'

export default {
  title: 'Atoms/Loading',
  component: Loading,
  parameters: {
    // layout: 'fullscreen'
  }
} as Meta

export const Default: StoryObj = {
  args: {}
}
